# LaTeXmk configuration file
#
# Usage example
# latexmk file.tex
#
# Main command line options
# -pdf : generate pdf using pdflatex
# -pv  : run file previewer
# -pvc : run file previewer and continually recompile on change
# -C   : clean up by removing all regeneratable files
#
# Use bibtex if a .bib file exists
# $bibtex_use = 1;
#
# $clean_ext = 'pdfsync synctex.gz synctex.gz(busy) acn acr alg aux bbl bcf blg brf dvi fdb_latexmk glg \
#   glo gls idx ilg ind ist lof log lot lox out paux pdfsync run.xml toc';
#
# $recorder = 1;
#
$pdf_mode = 1;
$pdflatex=q/xelatex -synctex=1 %O %S/
