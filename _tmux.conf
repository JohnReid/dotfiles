# From http://me.veekun.com/blog/2012/03/21/tmux-is-sweet-as-heck/

# Rebind prefix key
unbind C-b
set -g prefix C-p

# Set tmux to use vi keys
set-window-option -g mode-keys vi

# Make h,j,k,l move between panes
bind-key -r h select-pane -R
bind-key -r l select-pane -L
bind-key -r k select-pane -U
bind-key -r j select-pane -D

# Status bar has a dim gray background
set-option -g status-bg colour234
set-option -g status-fg colour0
# Left shows the session name, in blue
set-option -g status-left-bg default
set-option -g status-left-fg colour74

# window title
set-option -g set-titles on
set-option -g set-titles-string '[#S:#I #H] #W'

# Set the esc/alt disambiguation timeout to 50ms. The default is half a second, which makes vim kind of painful.
set-option -sg escape-time 50

# Mouse options
# https://bbs.archlinux.org/viewtopic.php?id=204091
set-option -g -q mouse on
# set-window-option -g mode-mouse copy-mode
# set-option -g mouse-resize-pane on
#set-option -g mouse-select-pane on
#set-option -g mouse-select-window on
# set-option -g terminal-overrides 'xterm*:smcup@:rmcup@'

#
# https://coderwall.com/p/4b0d0a/how-to-copy-and-paste-with-tmux-on-ubuntu
# Needs tmux version >= 1.8
# bind-key -T vi-mode y copy-pipe "xclip -sel clip -i"
# /home/jer15/.tmux.conf:42: usage: bind-key [-cnr] [-T key-table] key command [arguments]

# bind     -t vi-copy y copy-pipe "xclip -sel clip -i"
# bind C-v run-shell "xclip -sel clip -o | tmux load-buffer -; tmux paste-buffer"
# bind C-c run "tmux show-buffer | xclip -sel clip -i"
#
# Also see http://www.rushiagr.com/blog/2016/06/16/everything-you-need-to-know-about-tmux-copy-pasting-ubuntu/
# bind P paste-buffer
# bind-key -t vi-copy 'v' begin-selection
# bind-key -t vi-copy 'y' copy-selection
# bind-key -t vi-copy 'r' rectangle-toggle

# Copy/paste stuff - not needed - can use SPACE and ENTER
# bind-key -t vi-copy 'm' begin-selection
# bind-key -t vi-copy 'y' copy-selection

# Load X clipboard selection (+) into tmux buffer and paste
# bind C-v run "xsel -b | tmux load-buffer - ; tmux paste-buffer"
# Load X primary selection (*) into tmux buffer and paste
# bind M-C-v run "xsel | tmux load-buffer - ; tmux paste-buffer"
# Copy tmux buffer into X clipboard selection (+)
# bind C-c run-shell -b "tmux save-buffer - | xsel -ib"
# Copy tmux buffer into X primary selection (*)
# bind M-C-c run-shell -b "tmux save-buffer - | xsel -i"

# C-c: save into system clipboard (+). With preselection.
# bind C-c choose-buffer "run \"tmux save-buffer -b %% - | xclip -i -sel clipboard\" \; run \" tmux display \\\"Clipboard \(+\) filled with: $(tmux save-buffer -b %1 - | dd ibs=1 obs=1 status=noxfer count=80 2> /dev/null)... \\\" \" "
# # C-v: copy from + clipboard.
# bind C-v run "tmux set-buffer \"$(xclip -o -sel clipboard)\"; tmux paste-buffer" \; run "tmux display \"Copied from \(+\) $(xclip -o -sel clipboard | dd ibs=1 obs=1 status=noxfer count=80 2> /dev/null)... \""


# bindings to minimise and maximise a pane: makes it easier to use mouse select on remote tmux session
# from: http://superuser.com/questions/238702/maximizing-a-pane-in-tmux
# doesn't seem to work yet
#unbind +
#bind + new-window -d -n tmux-zoom 'clear && echo TMUX ZOOM && read' \; swap-pane -s tmux-zoom.0 \; select-window -t tmux-zoom
#unbind -
#bind - last-window \; swap-pane -s tmux-zoom.0 \; kill-window -t tmux-zoom

# Prevent renaming of windows.
# http://stackoverflow.com/questions/6041178/keep-the-windows-name-fixed-in-tmux
set-option -g allow-rename off
# http://superuser.com/questions/306028/tmux-and-zsh-custom-prompt-bug-with-window-name
set-window-option -g automatic-rename off
set -g default-terminal "screen-256color"
# Try to get 24-bit colours working
# Enable RGB colour if running in xterm(1)
set-option -sa terminal-overrides ",xterm*:Tc"

# From: http://unix.stackexchange.com/a/118381
# Opens new windows/panes in same directory as current pane.
#
bind '"' split-window -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"
bind c new-window -c "#{pane_current_path}"
